[1.题目背景请为跨境VOIP话务系统设计并实现一个最核心的通话费率计算引擎(VoIPCalc-Core)该引擎的职责非常单一:输入一次通话的基础信息(主叫、被叫、通话时间)，根据目的地国家、客户身份与通话时段，计算出本次通话的最终每分钟单价2.核心业务需求系统需要实现一个核心接口或方法:calculateRate(Callcontext context)，返回最终的每分钟单价。费用计算规则仅包含以下三条(可叠加):1.基础费率:由被叫号码的国家代码决定。中国(+86):&yen;0.10/分钟美国(+1):&yen;0.05/分钟其他国家/地区:统一默认&yen;0.50/分钟2.客户身份折扣:由主叫用户的身份决定。海外留学生/华人卡(vIP):享受9折(o.9)优惠普通用户(NoRMAL):无折扣3.夜间低谷福利:由通话发起的系统时间决定。如果通话开始时间在23:00至次日05:00之间，最终每分钟费率直接减免&yen;0.02(若减免后单价低于0元，则按0元计算)。DDD(领域驱动设计)请将业务概念显性化，核心逻辑必须是纯粹、无状态且无副作用的领域模型。],后端的jdk版本是21，采用springboot3.2.5，maven版本3.9.6，不适用框架依赖，确保代码干净，遵循ddd设计原则，注意边界值情况，采用 RESTAPI设计，前端用vue3要求：Apple 风格的极简黑白设计，主色调纯黑 和纯白卡片式布局，有轻微阴影数字和代码用等宽字体 结果切换有平滑动画响应式两栏布局

电话号码格式不太统一，有的是 +8613812345678，有的是 8613812345678，还可能有其他格式。怎么解析出国家代码最干净？

夜间时段 23:00-05:00 这个边界容易出问题。23:00 要算夜间，05:00 不算。还要处理跨天。怎么设计判断逻辑

现在有三个独立的计算规则：基础费率、客户折扣、夜间减免，我想把它们各自做成独立的策略类

计算结果除了返回一个数据，还需要把计算过程记录下来返回给前端保持不可变性就行

在导航栏右上角新增中英文按钮，采用i8n进行文本翻译

检查一下有没有错误处理，没有进行添加

进行表单验证

代码写完了，进行一遍代码审查。有没有 其他风险领域服务真的是无状态的吗领域层有没有不小心引入了框架依赖包结构符不符合 DDD 规范

